// === Firebase v9+ Modular SDK (CDN) ===
// 1) Create a project at https://console.firebase.google.com
// 2) Add a Web App, enable Email/Password Authentication
// 3) Replace the config below with your project's config
// 4) In Authentication > Sign-in method, enable Email/Password
// 5) In Firestore (optional), create a database if you want to store more data

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
